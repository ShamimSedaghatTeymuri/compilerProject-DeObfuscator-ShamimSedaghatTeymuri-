#include <stdio.h>
int main(){
    for (int number = 0; (number < 3); number = (number + 1)) {
        printf("%d\n", number);
}
    int number2 = 0;
    for (number2 = 0; (number2 < 3); number2 = (number2 + 1)) {
        printf("%d\n", number2);
}
    return 0;
}
